var g_config = {
	rpc: {
		host: window.location.hostname,
		port: window.location.port,
		path: '/jsonrpc',
	},
	music_lists: {
		/*
			If the size of the list is over this value the list is groupped by the first letter of the items
			false means disable grouping
		*/
        letter_grouping: {
			artists: 42,
			albums: 42,
			songs: false,
			directories: 42,
		},
		queue: {
			//auto jumps the highlight to the currently played item
			auto_jump: false,
		},
		//auto scrolls the highlight to the currently played item
		auto_scroll: false,
	},
};
